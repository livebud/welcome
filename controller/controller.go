package controller

type Controller struct {
}

func (c *Controller) Index() (string, error) {
	// cmd := exec.Command("bud", "version", "bud")
	// stdout := new(bytes.Buffer)
	// cmd.Stdout = stdout
	// cmd.Stderr = os.Stderr
	// if err := cmd.Run(); err != nil {
	// 	return "", err
	// }
	// return stdout.String(), nil
	return "", nil
}
